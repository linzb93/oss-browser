export interface AccountItem {
    /* 账号名称 */
    name: string;
    /* 平台 */
    platform: number;
    /* 区域 */
    region: string;
    /* 访问密钥ID */
    accessKeyId: string;
    /* 访问密钥密钥 */
    accessKeySecret: string;
    /* 存储桶 */
    bucket: string;
    /* 域名 */
    domain: string;
    /* 账号ID */
    id: number;
}
